# 📋 카페음료 만들기 안내 웹앱 핸드오프(Handoff) 문서

본 문서는 성인 발달장애인을 위한 **'차근차근 카페 레시피'** 웹 애플리케이션의 개발 및 기획 배경, 핵심 설계 원칙, 주요 기능 구현 내용 및 향후 유지보수를 위한 확장 가이드를 체계적으로 기록한 인수인계 문서입니다.

---

## 1. 소개 및 프로젝트 취지 (Introduction)
- **프로젝트명:** 차근차근 카페 레시피 (Step-by-Step Cafe Recipe Guide)
- **개발 환경:** 순수 HTML5, CSS3 (Vanilla CSS), JavaScript (ES6+ Module)
- **기획 목적:** 
  인지적 처리에 어려움이 있는 성인 발달장애인(지적장애, 자폐성장애 등)이 바리스타 훈련 및 실습 시 보조 교사의 개입 없이도 스스로 카페 음료를 처음부터 끝까지 제조하고 도구를 정리할 수 있도록 단계별 가이드를 제공하는 디지털 학습 보조 도구입니다.

---

## 2. 인지적 접근성 설계 원칙 (Accessibility Principles)
본 앱은 발달장애인 사용자의 사용성 평가 결과를 적극 반영하여 다음과 같은 특화 UX를 설계 및 구현했습니다.

*   **1화면 1단계 원칙 (Single Task per Screen):**
    여러 가지 정보가 얽혀 인지적 부담을 주는 것을 막기 위해 화면 하나당 하나의 개별 동작만 큼직하게 노출합니다.
*   **맥락 중심 레이아웃 (Context-First Layout):**
    사용자가 지금 해야 할 일을 먼저 텍스트로 인식하게 돕기 위해 **[단계 제목] ➡️ [동작 사진] ➡️ [상세 지시문]** 순으로 시선 흐름을 최적화했습니다.
*   **외래어 및 전문어 순화 (Plain Language):**
    캡(Cap) ➡️ *검정 뚜껑*, 플런저(Plunger) ➡️ *눌러내리는 막대*, 무게 재기 ➡️ *저울에 재기* 등 직관적으로 매칭할 수 있는 묘사 중심의 쉬운 한글을 채택했습니다.
*   **안전 타이머 모달 (Safe Timer Modal):**
    타이머 작동 중 실수로 창을 닫아 대기 흐름이 깨지는 것을 방지하기 위해 타이머 실행 중에는 ✕(닫기) 버튼을 강제로 숨깁니다.
*   **음성 안내(TTS) 기본 활성화 (TTS Active by Default):**
    글을 읽지 못하거나 시각 보조가 필요한 훈련생을 위해 진입 시 음성 안내 기능이 항상 켜져 있도록 설정했습니다.

---

## 3. 폴더 구조 및 파일 역할 (Project Structure)
```bash
recipe-guide/
├── index.html            # 메인 엔트리포인트 (DIV 앱 마운트 및 모듈 로드)
├── css/
│   └── recipe-guide.css  # 접근성 중심 테마 스타일 및 애니메이션 정의
├── js/
│   ├── recipe-data.js    # 레시피 데이터베이스 (단계 명칭, 지시문, 음성 등)
│   └── recipe-guide.js   # 코어 로직 (화면 라우팅, 타이머, TTS 제어)
└── assets/
    ├── aeropress/        # 아메리카노(에어로프레스) 단계별 최적화된 일러스트 JPG 파일들
    └── baedalwatsam/     # 배달왔삼 공식 브랜딩 자산 (달곰이 캐릭터 및 로고)
```

---

## 4. 핵심 기능 구현 명세 (Feature Specifications)

### ① 홈 화면(메뉴 선택) 및 진행도 초기화
- 사용자가 홈 화면에서 메뉴 카드를 클릭해 신규 제조 훈련에 진입할 경우, 기존의 진행 정보(`localStorage`)를 명시적으로 파괴하여 **언제나 첫 화면(시작/커버 페이지)에서 훈련이 깨끗하게 시작**되도록 보증했습니다.
- 반면, 실습 도중 실수로 브라우저를 새로고침(F5)하는 경우에는 훈련생이 길을 잃지 않도록 멈췄던 단계를 로컬 스토리지에서 다시 찾아 이어갈 수 있는 이중 안전장치를 탑재했습니다.

### ② 즉시 시작 모달 타이머
- 대기 시간이 필요한 '기다리기' 단계에 도달하면 별도의 사용자 조작 없이 **타이머가 즉시 자동으로 활성화되어 카운트다운을 시작**합니다.
- 모달 배경에 투명한 블러 필터를 씌워 뒷배경의 시각적 노이즈를 완벽하게 차단하고, 초시계와 진행률 막대바에 집중하게 만듭니다.

### ③ 음성 안내(TTS) 동기화
- `window.speechSynthesis` API를 활용하여 한글 안내 발화를 제어합니다.
- 음성 온/오프 상태가 페이지 최상단 전역 토글 버튼과 실시간으로 연동되어, 활성화 시 매 단계 전환마다 지시문을 알아듣기 쉬운 말(`ko-KR`, `rate: 0.9` 배속)로 재생합니다.

---

## 5. 이미지 에셋 최적화 및 스크립트 (Image Optimization)
- **문제 해결 사례:** 초기에 촬영 및 제공된 원본 일러스트레이션 PNG 파일(장당 1.2MB~1.7MB, 총 18MB)은 모바일 실습 현장에서 심각한 로딩 지연을 발생시켰습니다.
- **해결 방안:** PowerShell .NET 스크립트를 구현하여 이미지를 800px 가로 해상도로 조정하고, 흰색 배경 병합 처리를 더해 장당 **50KB~80KB 크기의 가벼운 JPG로 일괄 변환(총 용량 900KB 미만)**했습니다.
- **최적화 도구 위치:** `scratch/convert.ps1` 경로에 관련 일괄 처리 파워쉘 스크립트가 영구 백업되어 있어 차후 레시피 추가 시 재사용 가능합니다.

---

## 6. 향후 확장 가이드 (Future Extension Guide)
새로운 음료 메뉴(예: 준비 중인 바닐라라떼, 콤부차, 말차 바닐라라떼)의 실제 레시피 단계를 구현할 때는 다음 절차를 따릅니다.

1.  **에셋 추가:** 신규 일러스트 이미지를 800px 가로 크기의 JPG 포맷으로 만들어 `assets/<메뉴ID>/step-01.jpg` 형태로 배치합니다.
2.  **데이터 업데이트:** `js/recipe-data.js` 파일 내 해당 메뉴의 키 객체를 찾아 아래 포맷으로 단계 정보를 등록합니다.
    ```javascript
    kombucha: {
      id: "kombucha",
      title: "콤부차",
      coverImage: "assets/kombucha/cover.jpg",
      comingSoon: false, // false로 변경하면 홈 화면에서 '바로 시작' 배지와 함께 오픈됨
      steps: [
        {
          id: 1,
          title: "컵 준비하기",
          image: "assets/kombucha/step-01.jpg",
          instruction: ["준비된 투명한 컵에 물을 부어요."],
          speech: "준비된 투명한 컵에 물을 부어주세요.",
          type: "normal"
        },
        {
          id: 2,
          title: "기다리기",
          image: "assets/kombucha/step-02.jpg",
          instruction: ["30초 동안 기다려요."],
          speech: "삼십 초 동안 기다려주세요.",
          type: "timer",
          duration: 30
        }
      ]
    }
    ```

---

## 7. 인수인계 체크리스트 (Handoff Checklist)
- [x] 모든 UI의 색상 톤앤매너가 Teal 브랜드 컬러(배달왔삼 대표색 `#0D9488`)로 설정되어 있는지 점검 완료.
- [x] 타이머 실행 중 ✕ 버튼이 정상적으로 화면에서 보이지 않는지 확인 완료.
- [x] 음성 기능의 기본 동작 상태가 ON으로 켜지는지 확인 완료.
- [x] 새로운 메뉴 데이터 추가 시 `comingSoon: false` 설정으로 정상 연결되는지 구조 확인 완료.
